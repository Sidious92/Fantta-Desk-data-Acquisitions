name: Nexus FotMob PlayerStats Smoke v3
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.nexus-fotmob-playerstats-smoke-v3-launch/EXECUTE'
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests
      - run: sudo mkdir -p /mnt/data/nexus-fotmob-playerstats-smoke-v3 && sudo chown -R $USER:$USER /mnt/data
      - run: timeout 7m python scripts/probe_fotmob_playerstats_smoke_v3.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nexus-fotmob-playerstats-smoke-v3
          path: /mnt/data/nexus-fotmob-playerstats-smoke-v3
          if-no-files-found: warn
      - name: Record result
        if: always()
        run: |
          git fetch origin main
          git checkout -B main origin/main
          mkdir -p .nexus-fotmob-playerstats-smoke-v3-status
          if [ -f /mnt/data/nexus-fotmob-playerstats-smoke-v3/RESULT.json ]; then
            cp /mnt/data/nexus-fotmob-playerstats-smoke-v3/RESULT.json .nexus-fotmob-playerstats-smoke-v3-status/RESULT.json
          else
            printf '{"schema":"NEXUS_FOTMOB_PLAYERSTATS_SMOKE_V3","status":"NO_RESULT_FILE","run_id":%s}\n' "$GITHUB_RUN_ID" > .nexus-fotmob-playerstats-smoke-v3-status/RESULT.json
          fi
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .nexus-fotmob-playerstats-smoke-v3-status/RESULT.json
          git commit -m 'ci: record FotMob playerStats smoke v3' || exit 0
          git push origin HEAD:main
