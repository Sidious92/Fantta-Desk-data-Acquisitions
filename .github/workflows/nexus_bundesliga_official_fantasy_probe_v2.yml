name: Nexus Bundesliga Official Fantasy Public Probe v2
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.nexus-bundesliga-official-fantasy-probe-v2-launch/EXECUTE'
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests
      - run: sudo mkdir -p /mnt/data/nexus-bundesliga-official-fantasy-probe-v2 && sudo chown -R $USER:$USER /mnt/data
      - run: timeout 15m python scripts/probe_bundesliga_official_fantasy_v2.py
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nexus-bundesliga-official-fantasy-probe-v2
          path: /mnt/data/nexus-bundesliga-official-fantasy-probe-v2
          if-no-files-found: warn
      - name: Record result
        if: always()
        run: |
          git fetch origin main
          git checkout -B main origin/main
          mkdir -p .nexus-bundesliga-official-fantasy-probe-v2-status
          if [ -f /mnt/data/nexus-bundesliga-official-fantasy-probe-v2/RESULT.json ]; then
            cp /mnt/data/nexus-bundesliga-official-fantasy-probe-v2/RESULT.json .nexus-bundesliga-official-fantasy-probe-v2-status/RESULT.json
          else
            printf '{"schema":"NEXUS_BUNDESLIGA_OFFICIAL_FANTASY_PUBLIC_PROBE_V2","status":"NO_RESULT_FILE","run_id":%s}\n' "$GITHUB_RUN_ID" > .nexus-bundesliga-official-fantasy-probe-v2-status/RESULT.json
          fi
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .nexus-bundesliga-official-fantasy-probe-v2-status/RESULT.json
          git commit -m 'ci: record Bundesliga official fantasy public probe v2' || exit 0
          git push origin HEAD:main
