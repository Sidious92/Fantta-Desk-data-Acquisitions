name: Nexus GOALS Simple v1 Status

on:
  workflow_run:
    workflows: ["Nexus GOALS Simple v1"]
    types: [completed]

permissions:
  contents: write

jobs:
  record:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Record terminal status
        env:
          RUN_ID: ${{ github.event.workflow_run.id }}
          CONCLUSION: ${{ github.event.workflow_run.conclusion }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
        run: |
          set -euo pipefail
          mkdir -p .nexus-goals-simple-v1-status
          printf '{"workflow":"Nexus GOALS Simple v1","run_id":%s,"conclusion":"%s","head_sha":"%s"}\n' "$RUN_ID" "$CONCLUSION" "$HEAD_SHA" > .nexus-goals-simple-v1-status/RESULT.json
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .nexus-goals-simple-v1-status/RESULT.json
          git commit -m 'ci: record simple GOALS v1 status' || exit 0
          git push
