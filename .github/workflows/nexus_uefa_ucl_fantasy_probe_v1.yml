name: Nexus UEFA UCL Fantasy Public Probe v1
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.nexus-uefa-ucl-fantasy-probe-v1-launch/EXECUTE'
permissions:
  contents: write
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install requests
      - run: sudo mkdir -p /mnt/data/nexus-uefa-ucl-fantasy-probe-v1 && sudo chown -R $USER:$USER /mnt/data
      - run: python scripts/probe_uefa_ucl_fantasy_v1.py
      - uses: actions/upload-artifact@v4
        with:
          name: nexus-uefa-ucl-fantasy-probe-v1
          path: /mnt/data/nexus-uefa-ucl-fantasy-probe-v1
      - name: Record result
        if: always()
        run: |
          git fetch origin main
          git checkout -B main origin/main
          mkdir -p .nexus-uefa-ucl-fantasy-probe-v1-status
          cp /mnt/data/nexus-uefa-ucl-fantasy-probe-v1/RESULT.json .nexus-uefa-ucl-fantasy-probe-v1-status/RESULT.json
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add .nexus-uefa-ucl-fantasy-probe-v1-status/RESULT.json
          git commit -m 'ci: record UEFA UCL Fantasy public probe' || exit 0
          git push origin HEAD:main
