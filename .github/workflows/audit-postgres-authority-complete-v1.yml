name: Audit Postgres Authority Complete v1

on:
  workflow_dispatch:
  push:
    paths:
      - '.github/workflows/audit-postgres-authority-complete-v1.yml'

permissions:
  contents: read

jobs:
  audit-postgres-authority:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Require private-repo token
        env:
          FANTADESK_READ_TOKEN: ${{ secrets.FANTADESK_READ_TOKEN }}
        run: test -n "$FANTADESK_READ_TOKEN"

      - name: Checkout exact private FantaDesk SHA
        uses: actions/checkout@v4
        with:
          repository: Sidious92/FantaDesk
          ref: f8907b0dc10221173d76fa0d2b0a3b0a211b544d
          token: ${{ secrets.FANTADESK_READ_TOKEN }}
          path: app
          persist-credentials: false
          fetch-depth: 1

      - name: Assert exact private SHA
        working-directory: app
        run: test "$(git rev-parse HEAD)" = "f8907b0dc10221173d76fa0d2b0a3b0a211b544d"

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22.23.2

      - name: Install dependencies
        working-directory: app
        run: npm install --legacy-peer-deps --no-package-lock

      - name: Run authority tests
        working-directory: app
        run: npx vitest run src/features/auction/auctionSession.test.ts src/features/auction/auctionLedger.test.ts src/features/auction/nominationAdvisorPresentationV01.test.ts src/features/targets/targetRepository.test.ts src/features/auction/injurySnapshot20260912.test.ts src/features/settings/leagueRules.persistence.test.ts --reporter=verbose

      - name: Typecheck
        working-directory: app
        run: npx tsc --noEmit

      - name: Build web app
        working-directory: app
        run: |
          npm run nexus:runtime:build
          npx tsc -b
          npx vite build
