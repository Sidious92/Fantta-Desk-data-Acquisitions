name: Nexus D1 Understat Exact ID Identity Probe v1

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.nexus-d1-understat-exact-id-probe-v1/EXECUTE'

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v6
        with:
          python-version: '3.12'
      - name: Probe exact Understat IDs only
        run: |
          set -euo pipefail
          rm -rf .nexus-d1-understat-exact-id-probe-v1/output
          python scripts/nexus-d1-probe-understat-exact-id-identity-v1.py | tee /tmp/probe.log
      - name: Persist probe evidence
        run: |
          set -euo pipefail
          DEST=data/nexus-d1/understat-exact-id-probe-v1
          mkdir -p "$DEST"
          cp .nexus-d1-understat-exact-id-probe-v1/output/RESULT.json "$DEST/RESULT.json"
          cp /tmp/probe.log "$DEST/PROBE.stdout.log"
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add "$DEST"
          git commit -m 'nexus: persist D1 Understat exact-ID identity probe v1 outcome [skip ci]'
          git pull --rebase origin main
          git push origin HEAD:main
      - name: Upload raw probe evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nexus-d1-understat-exact-id-probe-v1
          path: .nexus-d1-understat-exact-id-probe-v1/output/
          retention-days: 30
